{
  "name": "wdd330final",
  "version": "1.0.0",
  "description": "tactical weather app for final project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html"
  },
  "author": "Brenden Lyon",
  "dependencies": {
    "parcel": "^2.13.2"
  }
}
